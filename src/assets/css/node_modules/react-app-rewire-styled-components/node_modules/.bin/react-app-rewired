#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_react-app-rewired@1.6.2@react-app-rewired/bin/index.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_react-app-rewired@1.6.2@react-app-rewired/bin/index.js" "$@"
  ret=$?
fi
exit $ret
